%h1 Nová rada

-#= error_messages_for :hint

- form_for(:hint, :url => collection_url) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Uložit"

= link_to 'Zpět', collection_url