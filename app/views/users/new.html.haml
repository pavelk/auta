#viewRecord
  .recordHeader
    %h2 Nová registrace
  %br
  %br  
  .recordPath
  .recordEdit  
    - form_for @object, :url => collection_url do |f|
      = f.error_messages
      %fieldset
        = render :partial => "form", :locals => { :f => f }
        %p= submit_tag "Uložit"
      %br
      * povinné položky